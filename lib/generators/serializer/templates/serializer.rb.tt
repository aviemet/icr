class <%= modelast_name.camelize %>Serializer < ApplicationSerializer
  object_as :<%= modelast_name %>

  attributes(<% model_attributes&.reject{ |attr| ["id", "updated_at", "created_at"].include?(attr) }&.each do |attribute| %>
    :<%= attribute %>,<% end %>
  )
end
