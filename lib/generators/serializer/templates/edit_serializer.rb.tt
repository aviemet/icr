class <%= modelast_name.pluralize.camelize %>::EditSerializer < <%= modelast_name.camelize %>Serializer

  attributes(<% if model_attributes.include?("slug") %>
    :slug,<% end %>
    :id,
    :updated_at,
    :created_at,
  )
end
