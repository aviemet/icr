---
en:
  active: Active
  activerecord:
    errors:
      messages:
        taken: There is already an account for this address, please log in instead
  admin: Admin
  application:
    invalid_authenticity_token_notice: The page expired, please try again.
  ends_at: Ends At
  errors:
    messages:
      accepted: Must be accepted
      blank: Can't be blank
      confirmation: Doesn't match %{attribute}
      empty: Can't be empty
      equal_to: Must be equal to %{count}
      even: Must be even
      exclusion: Is reserved
      greater_than: Must be greater than %{count}
      greater_than_or_equal_to: Must be greater than or equal to %{count}
      in: Must be in %{count}
      inclusion: Doesn't exist
      invalid: Not valid
      less_than: Must be less than %{count}
      less_than_or_equal_to: Must be less than or equal to %{count}
      model_invalid: 'Validation failed: %{errors}'
      not_a_number: Must be a number
      not_an_integer: Must be an integer
      odd: Must be odd
      other_than: Must be other than %{count}
      password_too_long: Too long
      present: Must be blank
      too_long:
        one: Too long (maximum is 1 character)
        other: Too long (maximum is %{count} characters)
      too_short:
        one: Too short (minimum is 1 character)
        other: Too short (minimum is %{count} characters)
      wrong_length:
        one: Wrong length (should be 1 character)
        other: Wrong length (should be %{count} characters)
  member: Member
  payroll:
    shift_exceptions:
      overlap: Overlaps with shift on %{time}
  permission_group: Permission Group
  pundit:
    not_authorized: You are not authorized to access this page.
  role: Role
  save: Save
  starts_at: Starts At
  templates:
    controllers:
      notices:
        created: "%{model} was successfully created."
        destroyed: "%{model} was successfully destroyed."
        status_updated: "%{model} status updated successfully"
        updated: "%{model} was successfully updated."
  timesheets:
    approval_window_closed: Approval is only allowed after the period has closed and before the payroll due date.
    approved: Timesheet approved.
    bulk_approved: "%{count} timesheet(s) approved."
